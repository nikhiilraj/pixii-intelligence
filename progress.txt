# Pixii Intelligence — build log

Read `## Codebase Patterns` first. Append what you did and what you learned after each slice.

---

## Codebase Patterns

### What this is

A standalone content-intelligence application that will replace `pixii-cli`,
`podcast-chopper`, `social-automation` and `ai-slop-scorer` (~30k LOC in
`~/monte-workshop/bots-and-tools/`). V1 is one vertical slice: Zernio corpus in → template
library → generate with lineage → Zernio draft → analytics joined back → dashboard.

Full spec: `.scratch/v1/spec.md`. Verified integration contracts: `.scratch/v1/seams.md`.
**Read both before the first slice.** They contain live-verified API behaviour that is not
guessable from documentation.

### Hard rules

- **Never write to `~/monte-workshop`.** Read-only reference only. This is a standing rule.
- **Nothing auto-publishes.** Every post reaches Zernio as a draft. This is permanent design,
  not a V1 limitation.
- **`.env` is a symlink to `~/Nikhil-workshop/.env`** — one source of truth. Never commit it,
  never write secret values into any file, never print a key value into logs or output.
- **Pin Python 3.12.** The system default `python3` is 3.14.0 and will be picked up silently.
- **Ponytail is on.** Laziest thing that works. No speculative abstractions, no interface with
  one implementation, no config for a value that never changes. Mark deliberate
  simplifications with a `ponytail:` comment naming the ceiling and the upgrade path.

### Stack

Backend: Python 3.12, FastAPI, Pydantic v2, SQLModel over SQLAlchemy, Alembic.
Frontend: Next.js 15 App Router, TypeScript, Tailwind, shadcn/ui, Recharts.
Database: PostgreSQL via Docker Compose (daemon confirmed running 2026-07-29).
Scheduling: APScheduler in-process. No Redis, no Celery, no broker in V1.
Package management: `uv` for Python, `pnpm` for Node. Both confirmed installed.

### The Zernio API — verified live 2026-07-29, do not re-derive

Base `https://getlate.dev/api/v1`, bearer auth with `ZERNIO_API_KEY`.
Zernio is the GetLate rebrand; `docs.getlate.dev` 301s to `docs.zernio.com`.

**`GET /v1/analytics` — two silent failure modes. Both return HTTP 200 with an empty list:**

- `limit` caps at **50**. `limit=200` returns zero posts and no `pagination` object.
- A `fromDate`/`toDate` span beyond **90 days** returns zero posts.

Page with `limit=50` and an incrementing `page`. **Treat an unexpected empty result as a
failure, never as an empty account.** The previous tooling carries a stale comment claiming
this endpoint does not exist at all; that class of silent, undetectable wrongness is exactly
what these tests guard against.

Per-post fields: `_id`, `latePostId`, `content`, `publishedAt`, `scheduledFor`, `status`,
`platform`, `platformPostUrl`, `mediaItems`, `thumbnailUrl`, `mediaType`, `profileId`,
`isExternal`, `isAd`, and `analytics{impressions, reach, likes, comments, shares, saves,
clicks, views, follows, engagementRate, lastUpdated}`. The `platforms[]` array repeats metrics
per account and carries `platformPostId` (`urn:li:share:…`) and `syncStatus`.

**`POST /v1/posts`** accepts `content`, `title`, `mediaItems`, `platforms`, `scheduledFor`,
`publishNow`, `isDraft`, `timezone`, `tags`, `hashtags`, `mentions`, **`metadata` (arbitrary
object — this is where lineage goes)**, and an optional `x-request-id` header for idempotency.
Returns `post._id`. **Draft is the default** when none of `scheduledFor`, `publishNow` or
`queuedFromProfile` is given. `POST /v1/posts/{id}/metadata` patches metadata later.

**Unresolved, and US-010 must resolve it:** the analytics payload carries both `_id` and
`latePostId` with different values. Which one the create response's `_id` corresponds to is
unknown and only determinable by creating a real post. Record the answer here when found.

### Other verified endpoints

Azure OpenAI chat — `POST {AZURE_OPENAI_CHAT_ENDPOINT}/openai/deployments/{deployment}/chat/completions?api-version={ver}`,
header `api-key`. Deployment `gpt-5-5`, api-version `2024-12-01-preview`. **Uses
`max_completion_tokens`, not `max_tokens`.** Verified HTTP 200.

Cloudflare Browser Rendering — `POST https://api.cloudflare.com/client/v4/accounts/{CLOUDFLARE_ACCOUNT_ID}/browser-rendering/screenshot`,
bearer `CLOUDFLARE_BROWSER_RENDERING_TOKEN`, body `{html, viewport:{width,height}, screenshotOptions:{type}}`.
Verified: returned a 1080×1350 PNG rendering `$325M` crisply on `#F5F0E8`.

Azure OpenAI image — credentials present for default plus POL/SWE/UAE/WUS3 regions.
**Not yet exercised live.** US-008 must verify it first.

### Corpus facts

50 posts available: 27 LinkedIn (Monte Desai), 13 YouTube (pixii.creates), 10 X (Pixii_ai),
spanning 2026-04-30 → 2026-07-28. 25 of 27 LinkedIn posts carry media. Average LinkedIn post
is ~818 characters. All 50 are `isExternal: true, syncStatus: "synced"`, so posts written
outside the app are included.

LinkedIn impressions, sorted: 6691, 5747, 4698, 3823, 2840, 1209, 1094, 928, 855, 800, 772,
640, 545, 525, 468, 457, 430, 405, 402, 394, 377, 354, 348, 332, 332, 111, 0.
Median 525, max 6691 — a **12.7x spread**.

**Primary success metric is engaged actions = likes + comments + shares + saves.** Not
impressions (the 6691 post drew 11 reactions), not engagement rate (a 332-impression post
scores 3.01). The two alternatives disagree on 60% of the top five.

**This is why V1 is a scoreboard, not an optimizer.** ~3 samples per template against a 12.7x
noise floor cannot support ranking. Show aggregates with their sample counts, flag thin
samples, let a human promote or retire. Do not build automatic template selection.

### Brand

Post visuals are 1080×1350 (4:5). Cream background `#F5F0E8`, heavy black type, orange Pixii
wordmark bottom-right. The established layout is an oversized ghosted figure behind a subject
image with a bold two-line headline beneath. Reference images from the real corpus are useful
for authoring visual templates — they are downloadable from `media.zernio.com` URLs in the
analytics payload.

Visual templates declare `renderer: "html" | "ai"`. Typographic frames use `html` because
image models garble large embedded text. Scene and lifestyle imagery uses `ai`.

### Missing corpus inputs — do not block on these

The creator posts Monte sent over LinkedIn, and the per-post design specifications, are not
in any repository or API. Extraction runs on the 27 available LinkedIn posts. Manual corpus
ingest exists so both can be absorbed later. **No slice's acceptance criteria may depend on
material that does not yet exist.**

---

## Session log

(append per slice: what was built, what was learned, what surprised you)

### US-001 — Walking skeleton (done 2026-07-29)

Built by the orchestrator rather than a loop iteration: it scaffolds two toolchains and is
the least tracer-bullet-shaped slice in the list.

Delivered: FastAPI backend on 8000 with `/health` reporting database reachability and
credential presence (presence only, never values); Postgres 17 in Docker on **port 5433**
(not 5432 — avoids collision with the Homebrew postgresql@16/@17 installs on this machine);
Alembic wired with baseline `c2756fd0ca1a`; Next.js 15 + Tailwind 4 frontend on 3000 whose
status page renders `/health` live; GitHub Actions CI running ruff, mypy, alembic and pytest
for the backend and tsc, eslint and build for the frontend; a Makefile where `make check`
runs everything.

Verified live end to end: `/health` returned `database: true` with all four credential groups
present, and the rendered page showed six green rows and no red.

Learned:
- `.env` is a symlink to `~/Nikhil-workshop/.env` and loads correctly through
  pydantic-settings from the repo root. Do not copy secrets into this repo.
- `uv venv --python 3.12` pins correctly; the machine default `python3` is 3.14.0.
- Alembic 1.14 needs `path_separator` in alembic.ini; the older `version_path_separator`
  spelling is deprecated.
- `create-next-app` produces Tailwind **4**, which has no `tailwind.config.ts` — configuration
  lives in CSS via `@import "tailwindcss"`. Do not add a v3-style config file.
- FastAPI's TestClient warns that `httpx` is deprecated in favour of `httpx2`. Harmless today;
  revisit if the warning becomes an error.
- Compose uses `restart: unless-stopped` so a Docker Desktop restart does not silently kill
  long unattended runs.

### US-002 — Corpus ingest from Zernio (done 2026-07-29)

Delivered: `app/zernio.py` (paging client, `ZernioResponseError`), `app/models/post.py`,
`app/corpus.py` (`ingest_posts` upsert), `GET /posts` and `POST /corpus/ingest`, migration
`b3784bcd19d0`, and a `/posts` page ranking the corpus by engaged actions.

18 tests green, ruff and mypy clean. Verified live against the real API: first ingest
created 50, second created 0 and updated 50. Top of the ranking reproduces the corpus
exactly — 185, 139, 48, 26, 20 — and the 6691-impression post with 11 reactions correctly
does not appear in the top five, which is the whole point of the metric choice.

Learned:
- **The empty-response guard keys on the missing `pagination` object**, not on a zero-length
  post list. A genuinely empty account still returns `pagination`; a rejected request does
  not. That distinction is what makes the failure detectable at all.
- Alembic's `script.py.mako` needed `from sqlalchemy.dialects import postgresql` added —
  autogenerate emits `postgresql.JSONB` for JSONB columns but does not import it, so the
  first migration with a JSON column fails with `NameError`. The template is now patched, so
  later migrations inherit the fix.
- Ruff's `B008` fires on FastAPI's `Depends()` in argument defaults. Resolved with
  `SessionDep = Annotated[Session, Depends(get_session)]` — the modern FastAPI idiom, and it
  satisfies the rule rather than suppressing it. **Reuse `SessionDep` for new endpoints.**
- Tests use a transaction-rollback `session` fixture (`tests/conftest.py`) against the
  migrated database, and `app.dependency_overrides[get_session]` to give the API that same
  session. Remember to clear the override at the end of an API test.
- httpx `MockTransport` is how the Zernio client is tested without a network call. Follow
  this pattern for the Azure and Cloudflare adapters.

### US-003 — Media download and post detail view (done 2026-07-29)

Delivered: `app/media.py` (`download_post_media`), `local_media_path` on Post (migration
`3ea8ff85c496`), media served from `/media` via StaticFiles, `GET /posts/{id}`, and a
`/posts/[id]` detail page with a metrics grid, the media, and the full body. The corpus list
now links through to it.

28 tests green, whole gate clean. Verified live: 40 media files / 31 MB downloaded — which
matches the corpus exactly, since 10 of the 50 posts are text-only (24 image + 16 video + 10
text). `/media/<file>` served the 2,497,633-byte hero PNG at HTTP 200.

Learned:
- **Media download is opt-in via `ingest_posts(..., with_media=True)`.** Tests and
  metric-only refreshes stay offline by default; only the API route asks for media.
- Every media failure is logged and swallowed, returning None. A post is complete without
  its image, and losing an image must never cost the post.
- **The `session` fixture now truncates all tables inside its rolled-back transaction.**
  A live ingest commits 50 real rows, and tests that count the table were seeing them
  (`assert 62 == 12`). Truncate-then-rollback gives a clean slate without deleting real data.
  Any new test that counts rows relies on this.
- Alembic autogenerate emits `op.add_column` on one long line that trips ruff's E501. Wrap
  the generated call by hand; it is the routine cost of a new column.
- `next/image` is wrong for backend-served media at arbitrary paths — use a plain `<img>`
  with an eslint-disable directly above the tag. A disable comment separated from the tag by
  another comment line does not apply and becomes an "unused directive" warning.
- When checking rendered HTML, `grep -c` counts *lines* and Next.js emits one long line.
  Use `grep -o ... | wc -l` or the count is misleadingly 1.

### US-004 — Template library: model, versioning, lifecycle, editor (done 2026-07-29)

Delivered: `app/models/template.py` (one table, `kind` discriminator, JSONB body),
`app/templates.py` (create / edit / approve / retire / `latest_versions` /
`usable_templates`), `app/api_templates.py` router, `app/deps.py`, migration `4cdc7e782318`,
and a `/templates` page with an author-and-revise form plus approve/retire actions. Added
nav across all pages.

46 tests green, whole gate clean. Lifecycle verified live end to end: proposed → 0 usable;
approve → 1 usable; edit → v2 with v1 intact; retire → 0 usable; editing a retired template
→ HTTP 409; both versions still on record.

**Decisions worth knowing before building on this:**
- **`family_id` is the stable identity, not `id`.** Every version is its own row sharing a
  `family_id`. Performance attribution must key on `(family_id, version)` — never on `id`
  alone, or an edit silently detaches history.
- **An edited version inherits the status it was edited from.** A strategist editing an
  approved template in the UI *is* the approver, so demanding re-approval is friction
  without review. Extraction proposals still require explicit approval — that is where the
  "nothing unvetted" rule actually bites.
- **Retired templates are immutable** (`RetiredTemplateError` → 409). Author a new family
  instead of reviving one.
- `usable_templates(kind)` is the only thing generation may consume: newest version,
  approved, right kind. Use it rather than querying Template directly.

Learned:
- `SessionDep` now lives in `app/deps.py` so routers can share it. **Import it from there**,
  not from `app.main` — that would be a circular import.
- New routers go in `app/api_*.py` as an `APIRouter` and are included from `main`.
- Alembic writes enum columns on one long line that trips E501; wrap by hand, same as
  `add_column`.
- Checking rendered React output for a string built from an interpolation (`from {n} posts`)
  fails on a naive grep — SSR splits it with `<!-- -->` markers. Strip comments and tags
  before asserting on rendered text.

### US-005 — Hook extraction from the winning posts (done 2026-07-29)

Delivered: `app/llm.py` (`AzureChat` + an `LLM` Protocol), `app/extraction.py`
(`propose_hooks`), `POST /templates/extract/hooks`, `get_llm`/`LLMDep` in `app/deps.py`,
and an extract button on `/templates`.

65 tests green, whole gate clean. Run live against Monte's real 27 LinkedIn posts: **6 hook
templates proposed, 0 hallucinated provenance ids, 0 usable until approved.**

The proposals independently reconstructed the top three posts —
`ai-time-value-equation` (#1, 185 engaged), `dated-platform-countdown` (#2, 139),
`small-input-big-recurring-result` (#3, 48) — and found two patterns spanning *multiple*
posts (`headline-stat-reframe` from #6 and #9, `open-question-then-tested` from #7 and #12).
That cross-post generalisation is the signal that the ranking is doing real work.

Learned:
- **The `LLM` Protocol is the seam.** Anything with `complete_json(system, user) -> dict`
  satisfies it. Tests inject a plain stub; `AzureChat` itself is tested separately with
  httpx `MockTransport`. Reuse this shape for the image adapter.
- **`max_completion_tokens`, never `max_tokens`** — this deployment rejects the old
  parameter. Already in the seam notes; now enforced by a test.
- Models sometimes fence their JSON in ```json despite instructions. `_parse_json` strips
  the fence; unparseable output raises `LLMResponseError` rather than returning empty.
- **Provenance is filtered against the ids actually shown to the model.** An id the model
  invented is dropped rather than stored — provenance must stay checkable, and a
  hallucinated citation is worse than none.
- **Route order matters:** `/templates/extract/hooks` is declared before
  `/templates/{template_id}/approve`, or FastAPI tries to parse "extract" as an int and 422s.
- Inject external clients as FastAPI dependencies (`LLMDep`), not by constructing them
  inside the handler — otherwise the endpoint cannot be tested without a network call.

### US-006 — Post structures for offer-reward and deep-research (done 2026-07-29)

Delivered: `propose_structures` and `compatible_hooks` in `app/extraction.py`,
`POST /templates/extract/structures` (with `sample_size` and `focus`),
`GET /templates/{id}/compatible-hooks`, and an extract-structures button.

78 tests green, whole gate clean. Approved and live: **`ai-workflow-giveaway`
(offer-reward, 8 sections)** paired with `ai-time-value-equation`, and **`case-study-loop`
(deep-research, 6 sections)** paired with `small-input-big-recurring-result`.

**The finding that shaped this slice — worth carrying forward.** Extraction at the default
sample refused to produce a deep-research structure, twice. Not a bug: the corpus does not
contain a deep-research post that performed. Of 27 LinkedIn posts, 4 are research-shaped and
three of those rank #14, #20 and #26 — below the top-12 cutoff (engaged 11). Only rank #5
made the sample. **Ranking by engaged actions structurally hides post types that
underperform**, which is correct for "what works" and wrong for "cover this post type".
Hence `focus`: it names a type to describe and tells the model to say when the evidence is
thin. Use it whenever a wanted post type is absent from the default sample — and read the
absence as signal, not as failure.

Learned:
- **Cited names are resolved to `family_id` and unknown names are dropped**, exactly as
  provenance ids are. A structure pointing at an invented hook would break generation the
  first time anyone selected it.
- `compatible_hooks()` returns only *usable* hooks, so an unapproved or retired pairing
  silently disappears rather than offering something generation would reject.
- Structure extraction sends **whole posts** (`POST_CHARS = 2000`); hook extraction sends
  only the opening (`HOOK_CHARS = 220`). Different questions need different windows.
- `GET /{id}/compatible-hooks` 400s on a non-structure rather than returning an empty list —
  an empty list would read as "no pairings" instead of "wrong kind of thing".

### US-007 — Visual templates with the html renderer (done 2026-07-29)

Delivered: `app/rendering.py` (`fill`, `CloudflareRenderer`, `render_visual`),
`get_html_renderer`/`HtmlRendererDep`, `POST /templates/{id}/preview` returning image/png,
a preview button on `/templates`, and a real `stat-hero` template reproducing the brand
layout (cream field, ghosted figure, orange cross, bold headline, Pixii mark).

90 tests green, whole gate clean. Verified live: 1080x1350 PNG at HTTP 200; missing slot
422; non-visual template 400; an `ai`-renderer template 501 (dispatch follows the
declaration); an escaped `<script>` value rendered as visible text with the layout intact.

**Decisions worth knowing before building on this:**
- **Visual templates carry their HTML as data, not a component name.** The spec sketch said
  "component identifier"; a markup string in `body.html` is lazier and strictly better here —
  a new visual template needs no code and no deploy, it versions like any other template,
  and a designer can edit the CSS directly in the editor.
- **`str.format()` cannot be used to fill slots.** CSS is full of braces —
  `.card { color: red }` — and format() chokes on them. `fill()` does targeted replacement
  and then scans for leftovers with a deliberately narrow `{snake_name}` pattern.
- **Slot values are escaped; template chrome is trusted.** That is the trust boundary:
  chrome is authored by the team, values will come from a language model.
- **A leftover placeholder raises rather than rendering.** Shipping an image with a literal
  `{headline}` in it is worse than failing.

Learned:
- **Cloudflare Browser Rendering rate-limits (429) under rapid successive calls.** It first
  surfaced as an opaque 500; the preview route now maps upstream `httpx.HTTPStatusError` to
  502 naming the upstream status, so "rate limited, retry" is distinguishable from "broken".
  **Later slices that render in a loop must pace themselves or handle 429.**
- The preview UI fills slots from each slot's own `example`, so a preview shows what the
  template author considered representative rather than lorem text.

### US-008 — Visual templates with the ai renderer (done 2026-07-29)

Delivered: `AzureImageRenderer` and an `ImageRenderer` protocol in `app/rendering.py`,
dispatch in `render_visual`, `get_image_renderer`/`ImageRendererDep`, and Pillow for scaling.
`.scratch/v1/seams.md` updated — the image seam is now verified rather than assumed.

102 tests green, whole gate clean. Generated live through the app: a 1080x1350 product shot
on the brand cream, no text — exactly the case the ai path exists for.

**The contract, verified live — this was the one seam never exercised:**
- `POST {endpoint}/openai/deployments/gpt-image-2/images/generations?api-version=2025-04-01-preview`,
  header `api-key`, body `{prompt, n, size, quality}`. The PNG comes back under
  **`data[0].b64_json`**; this deployment never returns a URL.
- **Both dimensions must be divisible by 16.** `1080x1350` is rejected outright, so the brand
  size cannot be asked for. `snap_to_16` rounds up to `1088x1360` — exact 4:5 — and Pillow
  scales down to the declared size. No crop, no distortion. Arbitrary `/16` sizes are allowed,
  so this generalises to any declared dimensions.

**Decisions worth knowing before building on this:**
- **`fill(..., escape=False)` for prompts.** HTML-escaping a prompt would send `&amp;` to the
  image model. Markup escapes; prompts do not. Getting this backwards is silent — the image
  would just come out subtly wrong.
- **Dispatch is by capability, not by type.** `render_visual` checks the declaration, then
  checks the supplied renderer has the matching method. Handing it the wrong renderer raises
  rather than silently falling back to the other path.
- **The style reference is concatenated onto every prompt.** It is the only thing holding
  generated imagery to the brand between runs, so it lives on the template, not in the call.
- A refusal (moderation, quota) raises `ImageGenerationError` carrying the service's own
  message and surfaces as 502. A blank image would be the worst possible outcome here.

Learned:
- Pillow 12 moved the resampling constants: `Image.Resampling.LANCZOS`, not `Image.LANCZOS`.
- `Image.open()` returns `ImageFile`, and `.resize()` returns `Image` — reassigning to the
  same name fails mypy. Bind the resized result to a new name.

### US-009 — Directed generation with lineage (done 2026-07-29)

Delivered: `app/models/draft.py` (migration `1bbaa40f8b8c`), `app/generation.py`
(`suggest_templates`, `generate_draft`, `regenerate_text`, `regenerate_visual`),
`app/api_drafts.py`, and a `/studio` page showing the draft, its visual and its lineage.

118 tests green. Verified live end to end: suggestion picked `open-question-then-tested` for
a "we tested 40 listings" idea with a coherent reason; the draft came back in Monte's voice
(lowercase, fragment-heavy) and rendered a brand-exact visual. Lineage recorded as
`(family, version)` for all three templates. `zernio_post_id` stays None — nothing published.

**The defect this slice surfaced, and the rule that came out of it:**
The first live draft filled `left_image_url` / `right_image_url` with prose — "image CTR",
"keyword repetition". Cloudflare rendered the page, the `<img>` tags silently failed, and the
result was a brand-correct image with **two empty boxes and `visual_error: None`**. A
success-shaped wrong answer, which is worse than an error.

- **Slots now declare a `type`.** Only `text`/`number`/untyped are offered to the model, and
  any value it volunteers for another type is discarded (`_written_values`). An unfilled
  `image_url` then raises `MissingSlotValue`, which surfaces as `visual_error` — loud, not
  silent. **Any new visual template with an image slot must set `"type": "image_url"`.**
- There is no asset picker in V1, so image-slot templates cannot complete unattended. The
  live library gained `stat-card`, a text-only brand card, so a draft always gets a visual.

**Other decisions worth knowing:**
- **Lineage keys on `(family, version)`, never a row id.** A test asserts a later edit of the
  hook leaves the draft's recorded version untouched.
- **A failed visual never costs the words.** `_draw_visual` catches everything, records why,
  and leaves the text intact — the prose is the expensive part.
- Exemplars come from the chosen hook's own `provenance`, so a draft is grounded in the posts
  that actually justify the pattern rather than the corpus at large.
- A suggestion naming a template that does not exist falls back to the first approved one
  rather than failing the request.

### US-010 — Publish to Zernio as a draft with lineage in metadata (done 2026-07-29)

Delivered: `app/publishing.py` (`push_draft`, `lineage_metadata`), `create_post` and
`list_posts` on `ZernioClient`, `get_zernio`/`ZernioDep`, `POST /drafts/{id}/push`, and a
"Push to Zernio as draft" action in the studio.

131 tests green, whole gate clean. Verified live on the real account: draft
`6a691c6d95e614b6077edb22` created with `status=draft`, full lineage in its `metadata`, and
`tags=['pixii-intelligence']`. Pushing again returned unchanged and created no second post.

**THE JOIN KEY IS `latePostId`, NOT `_id`. This was the open question and the answer is
counterintuitive — get it wrong and US-011 attributes nothing, silently.**

The create response returns `_id`, and `GET /v1/posts` carries that same value as its `_id`.
But in the analytics payload that value appears as **`latePostId`**; `analytics._id` is a
separate analytics-row identifier. Across all 13 published posts on the account:
`/v1/posts._id` → `analytics.latePostId` matched **13/13**; → `analytics._id` matched **0/13**.

So `Draft.zernio_post_id` (the create-time `_id`) must be joined against
**`analytics.latePostId`**.

**Analytics does not contain unpublished drafts at all.** A pushed draft is invisible to
`/v1/analytics` until it is actually published, so US-011 must treat "pushed but no metrics
yet" as the normal state, not a failure.

Other decisions worth knowing:
- **Draft-ness is asserted two ways**: `isDraft: True` *and* omitting `scheduledFor` /
  `publishNow` / `queuedFromProfile`. Zernio infers draft from the omission; sending the flag
  as well makes the intent explicit rather than implied. Tests assert on both.
- **The idempotency key is derived, not generated** — `uuid5` over the draft id, so a retry
  after a timeout reuses the same `x-request-id` and cannot create a second post. A
  already-pushed draft short-circuits before calling Zernio at all.
- **Every post carries `source: pixii-intelligence` in metadata and a matching tag.** The
  account already held 37 drafts and 13 published posts before this slice; without a marker,
  the app's output would be indistinguishable from Monte's own.
- `GET /v1/posts` returns `metadata`; `GET /v1/analytics` does not. Use `/v1/posts` whenever
  lineage needs reading back from Zernio's side.

### US-011 — Scheduled analytics sync joined to lineage (done 2026-07-29)

Delivered: `app/models/metric.py` (`MetricSnapshot`, migration `4281be495457`),
`app/metrics.py` (`sync_metrics`, `record_snapshots`, `draft_for_post`,
`template_performance`), `app/scheduler.py` (APScheduler, interval job),
`POST /metrics/sync` and `GET /metrics/templates`.

144 tests green, whole gate clean. Verified live: two syncs produced **100 snapshots across
50 posts** — history accumulates rather than overwrites — and the scoreboard reports 22
template versions, every one at `n=0`, with **no ranking anywhere in the payload**.

**The join, stated once so it is never re-derived:**
`Draft.zernio_post_id == Post.late_post_id`. The create response's `_id` appears in the
analytics payload as `latePostId`; `Post.zernio_id` holds `analytics._id`, which is a
*different* identifier. A test (`test_joining_on_the_analytics_row_id_finds_nothing`) asserts
the two differ, so anyone who later joins on the wrong one fails loudly instead of silently
attributing zero to everything.

**n=0 everywhere is the correct answer right now, not a bug.** Analytics excludes unpublished
drafts entirely, so a pushed draft contributes nothing until Monte actually publishes it.
`test_a_pushed_draft_with_no_metrics_yet_is_not_an_error` pins this.

Other decisions worth knowing:
- **Snapshots append; the Post row keeps the latest.** The row makes sorting cheap; the
  snapshot table keeps the engagement curve, which is the part that cannot be recovered later.
- **Performance keys on `(family, version)`.** A test edits a template after attribution and
  asserts v1 keeps its sample and v2 starts at zero.
- **Posts without lineage are excluded from template aggregates but stay in post-level
  views.** Everything ingested from history, and everything Monte writes outside the app,
  falls in this category — it is data, just not evidence about a template.
- **The scheduler swallows and logs its own failures.** A scheduler that dies on one bad run
  stops refreshing silently, which is precisely the failure this system exists to fix.
  `enable_scheduler` defaults to False so no test suite ever starts a thread that calls Zernio.
- FastAPI's `@app.on_event` is deprecated; the scheduler starts from a `lifespan` handler.

### US-012 — Dashboard post explorer (done 2026-07-29)

Delivered: filtering, sorting and a `SORTABLE` allow-list on `GET /posts`;
`GET /posts/{id}/history` for the engagement curve; and a `/posts` explorer with channel,
template and date filters, sort control, a Recharts line chart and a scrollable table.

156 tests green, whole gate clean. Verified live: no filter → 50 posts; `platform=twitter`
→ 10; `platform=linkedin` → 27; `since=2026-07-01` → 10; an unknown sort field → **422**;
history for the top post returned its 2 snapshots.

Learned:
- **Sort fields are an allow-list, not a passthrough.** `getattr(Post, sort)` on an arbitrary
  string is both an injection surface and a silent-wrong-answer surface — falling back to a
  default would show a different ranking than the one asked for. Unknown field → 422.
- **React 19's `react-hooks/set-state-in-effect` rejects fetch-in-`useEffect`.** Filtering is
  a user action, so `apply()` refetches from the change handler instead. Simpler and it is
  what the rule is pushing toward. **Do not reach for `useEffect` to sync server state here.**
- Filtering by template family goes through `Draft.zernio_post_id in (...)` against
  `Post.late_post_id` — the same confirmed join as everywhere else. Posts without lineage
  cannot match, which is correct: they are not evidence about a template.
- Wide tables live in their own `overflow-x-auto` container so the page body never scrolls
  sideways at laptop width.

### US-013 — Template scoreboard with sample counts and thin-sample flags (done 2026-07-29)

Delivered: `settings.min_sample_size` (default 5), a `sufficient` property on
`TemplatePerformance`, both surfaced on `GET /metrics/templates`, and a `/scoreboard` page
grouped by kind with every row linking through to the posts behind it.

164 tests green, whole gate clean. Verified live: 22 template versions, threshold 5, all 22
correctly marked insufficient, and **no ranking key anywhere in the payload** — a test asserts
the absence of `rank`/`score`/`best`/`recommended` rather than trusting it stays that way.

**The distinction the flag exists to preserve:** `sufficient: false` says *not enough evidence
yet*, never *this template performs badly*. Conflating the two is how someone retires a
template that actually works on a sample of two. The UI says "too thin (n/5)", not "0
engaged", and templates are ordered by name within their kind — deliberately not by
performance, because ordering by a number is a ranking whatever you call the column.

Learned:
- The scoreboard carries `min_sample_size` on every row so the UI never hardcodes the
  threshold it is applying — change the setting and the explanation changes with it.
- Link-through reuses the `template_family` filter from US-012, so "show me the posts behind
  this number" is one hop and needs no new endpoint.

### US-014 — Autonomous generation run (done 2026-07-29)

Delivered: `app/autonomous.py` (`propose_topics`, `run_autonomous`), `app/notify.py`,
an autonomous scheduler job, and `POST /drafts/autonomous-run`.

**176 tests green — full suite. All 14 slices pass.**

Verified live: the run proposed 2 fresh angles from the corpus, wrote 2 drafts, 0 failed,
both marked `autonomous`, both carrying full lineage — and it chose *different* hooks per
topic (`headline-stat-reframe` and `open-question-then-tested`). `zernio_post_id` is None on
both. One draft opened "no dollar impact yet" rather than inventing a number, which is the
prompt's no-invented-statistics rule holding under unattended conditions.

**Two hard limits on what unattended generation may do:**
- **It never reaches Zernio.** Drafts land here for review; pushing stays a human act even
  though a Zernio draft would not itself publish. An unattended loop with write access to the
  live account is a different risk from one without, and there was no reason to take it.
- **`cap` is a hard ceiling**, checked before any work. `cap=0` produces nothing and calls
  no model. A scheduling fault, retry storm or runaway loop cannot flood the review queue.

Also: **`enable_autonomous` defaults to False.** Unattended generation is opt-in, so deploying
this does not silently start writing.

Learned:
- **A failure that prevents the run raises; a failure on one topic costs only that topic.**
  `NoUsableTemplates` is the exception — nothing approved means no topic can succeed, so it
  stops rather than failing n times identically. A test pins the flaky-topic case.
- **`notify()` never raises.** A notifier that can fail its caller turns "the job had a
  problem" into "the job crashed" — strictly worse, and precisely the silent-failure class
  this system exists to correct. It always logs; Teams is best-effort on top.
- Topics are grounded in the recent corpus and asked for angles *not already covered*, so
  unattended output follows from the real account rather than from nothing.

### US-015 — Manual corpus ingest (done 2026-07-29)

**Found by the post-build review, not by the plan.** Spec user stories 5 and 6 — dropping in
posts Zernio does not carry — were never implemented, while three progress entries and
several status reports claimed "the ingest layer accepts manual corpus drops". It did not.
The gap mattered because the two known-missing inputs (Monte's creator posts, the design
specs) were both supposed to arrive through it.

Delivered: `PostSource` on Post (migration `959fc1ba1cc5`), `add_manual_post`,
`POST /corpus/manual`, and a `source` filter on `GET /posts`.

**185 tests green — full suite. 15/15 slices pass.**

Verified live: added an external creator post, ran a full 50-post Zernio sync, and the manual
row survived untouched — 1 manual, 50 zernio. Empty content → 422.

Learned:
- **Upsert is now scoped to `source == ZERNIO`.** Without that scope a sync could match,
  overwrite or orphan a manual row that nothing upstream knows exists.
- Manual ids are namespaced `manual:<uuid>` so they can never collide with a Zernio id.
- **Alembic: `add_column` does NOT create a PG enum type** — `create_table` does it
  implicitly, which is why the `template` table worked and this did not
  (`type "postsource" does not exist`). Create it explicitly with
  `postgresql.ENUM(...).create(op.get_bind(), checkfirst=True)`, and give a `NOT NULL`
  column a `server_default` so existing rows can take it. **Every future enum column on an
  existing table hits this.**

### Correction — 2026-07-29: the account is 155 posts, not 50

A peer session probing the same account read-only caught an error in my reporting, and it
was right. Recording it because the wrong numbers reached both the seam notes and the user.

**What was wrong:** I read `GET /v1/posts?limit=50` without paging and reported "37 drafts
and 13 published posts" as the account. That was page 1 of 4. The account actually holds
**155 posts — 103 draft, 45 published, 5 partial, 2 failed**. The join proof was therefore
stated as "13/13" when the published set is 45.

**What the corrected evidence shows — the finding holds and is stronger:**
`/v1/posts._id` → `analytics._id` matches **0 / 45**. `/v1/posts._id` →
`analytics.latePostId` matches **34 / 45**; the other 11 are outside the analytics window,
not counter-examples. A cleaner proof than content-matching: `GET /v1/posts/{analytics._id}`
→ **404**, `GET /v1/posts/{analytics.latePostId}` → **200, same post**.

**The finding that actually changes what this system knows:**
`GET /v1/analytics` is a **window, not the account** — `total: 50`, one page, and only 37 of
those rows carry a `latePostId`. There are **34 published LinkedIn posts** but extraction only
ever saw **27**. Eleven published posts, all March–April 2026, have no analytics row at all.
**The corpus is a subset of the published history, and every "learned from the corpus" claim
in this log means the 50-row analytics window.** Widening it means paging `GET /v1/posts` and
accepting rows with no metrics.

**And a confirmation obtained without writing:** a pushed draft can never be verified against
analytics — 0 of 103 drafts appear there. The live post created for US-010 was not needed to
answer the id question; cross-referencing published posts settles it read-only. The ticket's
prescribed method ("create a post, then look it up in analytics") does not terminate.

**Lesson, and it is the same one twice now:** this API truncates silently in more than one
way. An unpaged `limit=50` looks exactly like a complete account, just as an over-limit
request looks exactly like an empty one. **Check `pagination.total` and `pages` before
believing any count from this API.**

### V2-1 — Page `/v1/posts` for the full published history (done 2026-07-29)

The corpus sourced posts only from `GET /v1/analytics`, which is a recent 50-row window
rather than the account. 34 published LinkedIn posts exist; 27 were in the corpus. The
missing ones carry no metrics but do carry text, and text is what extraction reads.

Delivered: a paged `ZernioClient.list_posts()` that walks `/v1/posts` to completion and
checks the collected count against the `pagination.total` the API itself reported;
`ingest_history` + `_history_payload` in `app/corpus.py`; and a second pass inside the
existing `POST /corpus/ingest` — analytics first, then history. No flag, no new endpoint,
no migration.

**`list_posts()` replaced the unpaged `list_posts(limit, page)` added by US-010.** It had
no callers anywhere in the repo — that older version *was* the page-1-of-4 trap.

**The two payload shapes are not the same, and this is the part that cost the time.**
`/v1/posts` names the post `_id` where analytics says `latePostId`; carries no `analytics`
block at all; and puts `platform`, `publishedAt`, `platformPostUrl` and the account name
on the per-platform entry instead of at the top level. `_history_payload` reshapes one into
the other so the single existing upsert serves both.

**Insert-only, and LinkedIn-only. Both are load-bearing:**
- Applying a metric-less history payload over a row analytics had already filled would
  zero it. A post already present — matched on `(latePostId, platform)` — is skipped.
- `late_post_id` is **not unique**: 3 crossposts each produce two analytics rows (LinkedIn
  and X) sharing one `latePostId`. Any upsert keyed on it would collapse them and make
  them flip-flop on every sync. Hence the composite match.
- The 13 youtube analytics rows carry **no `latePostId` at all**, so they cannot be
  matched against `/v1/posts` — backfilling other channels would have duplicated every
  video, plus added 4 instagram rows nobody asked for. LinkedIn only until the other
  channels get their own slice.

Verified live: `POST /corpus/ingest` returned
`{fetched: 50, created: 0, updated: 50, history_fetched: 155, history_recovered: 7}` —
the 155 confirms all four pages were walked. Corpus LinkedIn rows **27 → 34** and
`min(published_at)` moved **2026-06-05 → 2026-02-17**. The date shift is the real proof;
the count alone would not distinguish a recovery from a duplication. A second run
recovered 0.

The seven, all at `engaged_actions = 0`: 2026-02-17, 02-18, 03-30, 03-31, 04-01, 04-02 and
04-23 — exactly the seven a read-only probe of `/v1/posts` predicted, and all older than
the analytics window, which is why that window will never return them on its own.

**What this does not do, and it matters for the "cheapest quality jump" claim.**
Extraction selects its sample with `order_by(engaged_actions desc).limit(12)`
(`app/extraction.py:94`). The 7 recovered posts arrive at `engaged_actions = 0`, so they
rank last and **do not enter the default extraction sample**. They are reachable through
`focus` and a larger `sample_size`, and they are in the explorer, but widening the corpus
did not by itself widen what extraction sees. Ranking by a metric these rows do not have
is the reason.

Also: backfilled rows are never snapshotted — `sync_metrics` selects on
`Post.zernio_id in (analytics ids)` and these rows are keyed on the platform-entry id.
Correct, since they have no metrics to track, but it is why their history stays empty.

---

### LinkedIn profile scrape → corpus (`upsert_linkedin_posts`, `POST /corpus/linkedin`)

**The join key is normalised content, and the URN proves why.** The profile DOM names a post
`urn:li:activity:<id>`; Zernio's `platformPostId` names the same post `urn:li:share:<id>`.
Different namespaces — a URN join matches **0 rows**. `re.sub(r'\W+','',content).lower()[:120]`
matches, and absorbs the whitespace/punctuation drift of a post edited after publishing.
Verified offline against the live corpus before any code was written.

**Live load, 39 scraped posts: 4 matched existing rows, 35 created. LinkedIn corpus 35 → 70.**
Only **5** of the 39 fall inside the corpus's date window (2026-02-17 →), and 4 of those 5 hit;
the fifth (2026-07-17, "China's AI labs…") is genuinely absent from Zernio — nearest existing
row scores 0.31 similarity. The other 34 misses are all older than anything Zernio holds. So
the scrape's real value is **history**, not enrichment: it reaches back to **2024-01-25**.

**Repost counts only exist here.** Zernio reports `shares: 0` on every LinkedIn row; the scrape
carries 43 across 11 posts. Shares are in `_ENGAGED`, so a corpus where only some rows have them
mis-ranks — writing them is correctness, not a bonus. All 43 landed on created rows: none of the
4 merged posts had been reposted.

**A merge writes reactions only.** Impressions are author-only and invisible when scraping
someone else's profile, so a scraped row has none and writing them would replace a real figure
with a zero. `likes`/`comments` take `max(existing, scraped)` — the scrape is live, analytics
lag, neither side is authoritative. `impressions`, `engagement_rate`, `source` and `zernio_id`
are never touched; confirmed live (merged rows kept 210 / 470 / 332 / 1209 impressions).
Re-running the whole file: **0 created, 39 updated**, still 70 rows.

**This *does* change what extraction sees — unlike the `/v1/posts` backfill above.** Those rows
arrived at `engaged_actions = 0` and ranked last. These carry real reaction counts, and Monte's
2024 launch-era posts out-engage everything Pixii has published since: the top-12 sample
`_strongest_posts` feeds the LLM is now **8 scraped rows to 4 Zernio rows**, topped by a 2024
soft-launch post at 181. **Cross-era `engaged_actions` are not comparable** — a 2024 post drew a
different audience, and with no impressions on those rows nothing can normalise for it.
Re-extraction will now learn from 2024 personal/launch posts rather than 2026 Amazon-listing
posts. That is a judgement call for Nikhil, not a bug: it is the ranking key doing exactly what
it says.

**Media.** `media.licdn.com` serves images from paths with **no extension**, so `_filename`
stored them suffixless and StaticFiles handed them out as `application/octet-stream`. Suffix now
falls back to the response's content-type (gated through `_KNOWN_SUFFIXES`, so
`application/octet-stream` → `.bin` is rejected). Live: 10/10 image posts stored `.jpg`, and
`GET /media/linkedin:urn:li:activity:….jpg` returns `200 image/jpeg`. Video is **never
downloaded** — `media_type='video'` and the post URL, nothing else (11 posts). Carousels keep the
cover image only.

No migration: every column already existed, and no `PostSource` value was added — `alembic/env.py`
wraps migrations in a transaction, which makes `ALTER TYPE … ADD VALUE` a trap.

**Ceiling — the next Zernio sync wipes shares off a *merged* row.** `sync_metrics` calls
`ingest_posts`, which re-applies the analytics payload over every column in `_METRICS`; Zernio
reports `shares: 0` on every LinkedIn row, so a merged post still inside the 50-row analytics
window loses its repost count on the next scheduled sync and `engaged_actions` drops with it.
Rows the scrape *created* are safe — no sync knows them — and so is any post older than the
window, which is where all 43 reposts currently live. Pinned by
`test_the_next_zernio_sync_wipes_the_shares_a_merge_wrote`. The fix belongs in `_apply` (stop
trusting a zero for a metric the source never observes), which was out of scope here.

Provenance note: the input, `.scratch/corpus-widening/monte-linkedin-2024-2026.json`, and the
scraper, `scripts/linkedin_extract.js`, are **untracked** — 35 corpus rows currently have no
versioned source. `.scratch/v1/` is tracked, so committing them would be consistent.

---

## Scoping extraction to Monte's repeatable posts

`_strongest_posts` selects the sample the LLM learns hooks and structures from. The corpus
tripling (27 → 70) exposed three faults in that one query. All three are fixed **inside** the
query, before `.limit()` — the previous code filtered textless posts in Python *after* the
limit, so every discarded row silently cost a real post its slot.

**Voice.** The top row was a third-party creator post at `engaged_actions = 1240` — 6.7x
Monte's best — so templates claiming to describe *his* voice were led by someone else's
writing. `settings.voice_account` (default `"Monte Desai"`, pydantic-settings so it stays
overridable) now filters the sample. Creator posts stay in the corpus as reference material;
they just stop shaping voice templates.

**Repeatability.** New column `Post.excluded_from_extraction` (bool, not null, default false)
plus `POST /posts/{id}/exclude` and a toggle on the post detail page. Migration
`4a783e8a1648` is a plain `op.add_column` with `server_default=sa.false()`; no enum was
touched, for the reason recorded above. Excluded live: **id 3017** ("Soft launch to my
connections…", 181, 2024-01-25) and **id 3011** ("Breaking out of stealth…", 171,
2024-02-28) — Monte's two best posts ever, both winning on launch novelty and a network
firing once. The hook is not reproducible, so teaching it teaches a trick that only works
once. Both keep their rows and their metrics.

**The sample this produces** — 12 posts, all Monte, `engaged_actions` 185 → 29, spanning
2024-01 to 2026-07. The three ex-launch/creator slots were filled by posts at 32, 30 and 29,
so the tail is now genuinely thin: the sample runs down to posts with 29 engaged actions.
That is the corpus being honest about its size, not a regression.

**Ceiling — `add_manual_post` sets `account_username = author`, and `author` defaults to
`None`.** Any Monte post pasted at `/posts` without an explicit author now silently stops
shaping voice templates. `author` is optional in the API and the form, but is effectively
required in practice. Left alone deliberately this round.

Ranking metric untouched: `engaged_actions desc`. 223 tests, `make check` green.

**Second ceiling — extraction is LinkedIn-only now.** `voice_account` is one string, and the
account names differ per platform (`Pixii_ai` on twitter, `pixii.creates` on youtube), so
`POST /templates/extract/hooks?platform=twitter` returns zero proposals rather than an error.
Confirmed live: `_strongest_posts` returns 12 for linkedin, 0 for twitter and youtube. Marked
`ponytail:` in `config.py`; the upgrade is a per-platform mapping when X and Reddit land.

---

## Extraction: a publish-date floor on the voice sample

**Why.** The corpus was widened back to 2024, and Monte's 2024 posts are a different genre —
AI-industry commentary (Sora, GPT-4 model roundups, Google, Meta) rather than the
Amazon-listing content Pixii publishes now. Ranked on `engaged_actions` alone, six of the
top twelve were 2024 posts (43, 40, 32, 30, 29, 29), so the templates would have described a
voice the company no longer writes in. Cross-era engagement is not comparable either: those
posts reached a different audience, and the rows carry no impressions to normalise against.

**The filter.** New setting `voice_since: datetime = datetime(2025, 1, 1)` in `config.py`,
alongside `voice_account`. Applied in `_strongest_posts` as
`.where(col(Post.published_at) >= settings.voice_since)` — in the query, before the limit,
like every other exclusion, so an out-of-era post never costs a current post its slot.
A `NULL published_at` fails the comparison and is excluded by plain SQL three-valued logic;
no second predicate, but the behaviour is pinned by `test_an_undated_post_is_not_evidence`
so it can never drift silently. Undated is treated as not-current: an undated post cannot be
shown to belong to the present era.

**The sample this produces** — 12 posts, `engaged_actions` 185 → 20, spanning 2025-03 to
2026-07. The six 2024 slots were filled by posts at 26, 23, 23, 21, 21 and 20, so the tail
got thinner again (floor moved 29 → 20). Eligible pool: 54 of 67 non-empty, non-excluded
Monte LinkedIn posts; 13 fall before the floor, 0 are undated. A concurrent workstream is
still scraping 2025-08→2026-01 posts in, so these counts will move.

**Ceiling — undated manual pastes are now invisible to extraction.** `add_manual_post`
leaves `published_at` at `None`, so a Monte post pasted without a date stops shaping voice
templates, the same trap `author` already has. Left alone deliberately; the fix is to make
both effectively required at the API and form.

Ranking metric untouched: `engaged_actions desc`. 226 tests (223 + 3), `make check` green.

---

## Extraction: an inspiration cohort, and a hard line at the voice

**Why.** ~12 LinkedIn posts by other creators are being ingested under
`account_username = "Creator inspiration"`. They may teach hooks and post structures — a
hook is a borrowable shape — but they must never shape Monte's voice. Yesterday's
`voice_account` filter blocked them wholesale; this generalises it into a cohort so their
shapes are reachable, without opening the voice path.

**The cohort.** `extraction.Cohort` (`VOICE` | `INSPIRATION`), threaded through
`_strongest_posts`, `propose_hooks`, `propose_structures` and both extract endpoints
(`?cohort=inspiration`). Defaults to `VOICE` everywhere, so nothing about today's behaviour
moves. New setting `inspiration_account: str = "Creator inspiration"` beside `voice_account`.
One cohort per sample, never mixed — a mixed sample led by whichever cohort ranked higher
would describe neither.

`excluded_from_extraction` and the non-empty-content filter apply to both. The content
filter is load-bearing for inspiration specifically: at least one collected creator post is
an image with no text at all, and it outranks the rest, so it would lead the sample carrying
no hook. `voice_since` applies to VOICE only — it is an era floor on Monte's own history,
and the date his current voice began says nothing about someone else's timeline; the two
cohorts are never ranked against each other, so there is nothing to make comparable.

**Provenance — a deviation, flagged.** The brief said to record the cohort in the Template's
`provenance` JSONB. `provenance` is typed `list[str]` of post ids, has 28 live rows holding
bare arrays, is read positionally by `generation._exemplars`, and is rendered as
`t.provenance.length` in `TemplateManager.tsx`. Reshaping it to `{cohort, post_ids}` means
13 files plus an Alembic data backfill to keep the running app working. Cohort went into
`body` instead — also existing JSONB, no column added, no migration, no legacy row broken,
and already dumped verbatim in the template UI, so `"cohort": "inspiration"` is visible
today with zero frontend change. Say the word and it moves.

**The guard.** `generation._exemplars` is the one path that hands a real post to the model
as a voice to imitate. It now filters `Post.account_username == settings.voice_account` in
the query — a positive allowlist, not a claim the template makes about itself, so it holds
even for a hook whose provenance was hand-edited to cite a creator. Pinned by
`test_a_creator_post_is_never_handed_to_the_model_as_a_voice_to_imitate`: the creator post
is named in the hook's own provenance and outranks Monte's, and the assertion is that its
text never appears in the prompt. Verified by mutation — deleting the `where` fails that
test and its `regenerate_text` twin, not a SQL-string comparison.

Ranking metric untouched (`engaged_actions desc`), `voice_account`, `voice_since` and the
two hand-excluded posts untouched. 237 tests (226 + 11), `make check` green. Extraction has
not been run against the live LLM — Nikhil reviews the sample first.

---

## Corpus: the 14 creator inspiration posts, in the database

**Why.** They existed only as a parsed JSON file in `.scratch/`. Every post lives in the
database — that is the standing rule, and until they are rows the cohort filter added last
session has nothing to select.

`corpus.ingest_inspiration_posts` inserts them as `source=MANUAL`, `platform="linkedin"`,
`is_external=True`, `status="external"`, under **`settings.inspiration_account`** read from
settings rather than spelled out. That string is the whole join: `_strongest_posts` selects
on `Post.account_username == settings.inspiration_account`, so a hardcoded copy that drifts
by one character drops all 14 from extraction while the ingest still reports success.
`test_the_cohort_is_named_by_settings_not_by_hand` and
`test_an_ingested_creator_post_reaches_inspiration_extraction` pin it from both ends —
verified by mutation (a trailing space on the username fails two tests).

**Ids are `inspiration:linkedin-archive:<index>`** — the post's position in the document,
with the slug a **constant in `scripts/ingest_inspiration.py`, deliberately not derived from
the document's filename**: a rename or a re-parse must not mint 14 new ids. Insert-only, so
a second run creates 0 and disturbs nothing already there. ponytail: correcting a bad parse
therefore means deleting the rows and ingesting again, not re-running over the top.

**`published_at` is None.** The archive records no dates. `voice_since` gates on
`Cohort.VOICE` only, so undated inspiration rows are still evidence — the round-trip test
proves it rather than assuming it.

**Metrics — three posts' `engaged_actions` are lower bounds.** Each metric holds its own
figure (not `add_manual_post`'s `likes = engaged_actions` shortcut, which would make every
creator post read as unshared). **Posts 4, 9 and 11 record no share count** — *unrecorded in
the document*, not zero. The `Post` metric columns are non-null ints and no migration was
worth that distinction, so it is stored as 0 and their engaged actions (20, 415, 27) are
floors. The nulls themselves survive in the committed
`.scratch/corpus-widening/creator-inspiration.json`.

**Images.** The 12 extracted PNGs are copied from `.scratch/corpus-widening/creator-images/`
into `settings.media_dir` as `inspiration_linkedin-archive_<index>.png` — the post id with
its colons flattened, so the name needs no quoting in a URL path. **Nothing is
re-downloaded**: the `media.licdn.com` addresses the document annotates two posts with have
expired, and fetching one would replace a file we have with a 403. `media_items` is left
empty for the same reason — there is no URL for anything to retry.

Live run: 14 created, corpus 93 → 107. Second run: `created=0 skipped=14`. 252 tests
(237 + 15), `make check` green.

**Open, for Nikhil.** One older row sits under `account_username = 'External creator'`
(`engaged_actions` 1240) — a creator post that predates this cohort and is invisible to
`Cohort.INSPIRATION` because the username does not match. It also carries the
`likes = engaged_actions` shortcut, so its `likes` figure is not real. Left untouched:
folding it in means deciding what its real metrics were.

**A second caveat, same shape as the shares one.** Posts 11 and 12 carry parser `notes`:
the archive's `Post-` delimiter for 12 was pasted a few lines late, so **post 12's opening
lines were moved back from post 11**. Post 12 is now the top-ranked inspiration post (4331
engaged actions) and `_hook_of` reads a hook from exactly those opening lines — so the
strongest hook in the cohort rests on that boundary judgment, and post 11 is left 81
characters long. The metrics were audited against the document; the content boundaries were
not. Both notes survive in the committed JSON.

Two consequences worth knowing. `DEFAULT_SAMPLE_SIZE` is 12, so the default inspiration
sample takes 12 of the 14 — indices 1 (12 engaged actions) and 4 (20) fall below the cut.
All 14 are eligible; nothing is excluded. And `/posts` sorts by `engaged_actions` with no
account scope, so the default corpus view is now led by creator posts (4331, 1195) sitting
far above Monte's previous top of ~185. Cosmetic only — `template_performance` requires
lineage, so no aggregate moves.

### Cohorts in the UI — account filter, scoped corpus, cohort-aware extraction (done 2026-07-30)

The backend had grown three cohorts and the frontend still showed one undifferentiated
corpus. Four gaps closed: an `account` filter on `/posts`, a cohort selector on the corpus
view defaulting to our own posts, a voice/inspiration choice on both extract buttons, and
each template's cohort surfaced while reviewing it.

Delivered: `account: str | None` on `list_posts` (exact equality, matching
`extraction._strongest_posts` — the UI's idea of a cohort must be the same rows extraction
reads); a cohort `<select>` plus an `Account` column in `posts/Explorer.tsx`; a cohort
`<select>` feeding both extract calls in `templates/TemplateManager.tsx`; and a `CohortTag`
reading `body.cohort`.

Learned:

- **`source` cannot separate the cohorts and never could.** Monte's 35 scraped posts and
  the 14 creator posts are both `MANUAL`. The account is the only discriminator, which is
  why the filter had to be added rather than the existing one reused. The test asserts both
  halves: `account` separates them, `source=manual` returns both.
- **The corpus holds more accounts than cohorts.** `Monte Desai` 69 (35 MANUAL + 34 ZERNIO),
  `Creator inspiration` 15, `pixii.creates` 13, `Pixii_ai` 10 — six accounts mid-slice, four
  by the end: the legacy `External creator` row folded into `Creator inspiration` (14 → 15)
  while this was being built. **The 23 YouTube and X rows belong to neither cohort**, so a
  two-option toggle would have made them unreachable from the browser — the same bug class
  as the unscoped ranking it was sent to fix.
  The selector derives its options from the corpus itself and adds an "all accounts" entry;
  only the two cohort names are constants.
- **The default view is scoped by filtering `initial`, not by refetching.** `page.tsx` still
  fetches all 107 so both dropdowns can be derived from the whole corpus; `Explorer` seeds
  its state with `initial.filter(...)`. Scoping the server fetch instead would collapse the
  platform list to `["linkedin"]` and leave the account list underivable — and refetching
  on mount is what React 19's `set-state-in-effect` rule forbids.
- **`body.cohort` is absent on most of the queue, and blank is not a safe rendering.** Only
  `_to_template` and `_to_structure` write it, so anything authored through the form — or
  extracted before the field existed — has no key, and `String(t.body.cohort)` would render
  the literal `"undefined"`. But rendering *nothing* is also wrong: on the live page 10 rows
  are tagged `borrowed from a creator` and 38 are not, so a blank beside them reads as
  "not borrowed" — i.e. proven in our own posts, which those rows do not claim. **Three
  states, three renderings**; the absent one says `cohort unrecorded`. Gap 4 exists so a
  reviewer can tell borrowed from proven, and a blank leaves them guessing on the majority.
- **Both extract query strings are built with `URLSearchParams`.** Hooks carries no param
  and structures already carries `sample_size`, so `?` vs `&` differs between the two —
  concatenating by hand ships one working button and one 422.
- The cohort goes over the wire as the plain string `"voice"` / `"inspiration"` and FastAPI
  coerces it into the `Cohort` StrEnum. No new representation.

**Noticed, not fixed — `sample_size=27` in the structures button is stale.** It was the
LinkedIn post count when that button was written; the voice cohort is now 69 posts and the
inspiration cohort is 14, for which 27 is simply "all of them". Outside these four gaps, so
left alone.

### Three false values removed — untyped slots, a fabricated likes count, the hack behind it (done 2026-07-30)

Not imprecision. Each of the three reported something as measured or usable that was neither.

Delivered: `""` out of `WRITABLE_SLOT_TYPES`; `post.likes = engaged_actions` out of
`add_manual_post`; `scripts/correct_external_creator_metrics.py` and
`scripts/fix_untyped_visual_slots.py`. 258 tests green, whole gate clean.

**Was the `stat-hero` v1 bug live or latent? Both, on different paths — and this is the
part worth remembering.**

`usable_templates` selects **newest version per family, then filters to APPROVED**
(`_latest_version_ids` groups on `max(version)`). v1 (556) and v2 (1101) share family
`a2bcf8e2`, so v1 has been unreachable to suggestion since v2 was authored. On that path:
**latent**. Retiring 556 therefore changes the `usable_templates` output by exactly nothing —
it is hygiene, not the fix.

But `generation._resolve` does `session.get(Template, id)` and checks **`kind` only** — no
status, no newest-version — and `api_drafts.IdeaIn` takes a caller-supplied `visual_id`. So
`POST /drafts {"visual_id": 556}` reached v1 **today**, and still would after retirement,
because retirement sets a status `_resolve` never reads. On that path: **live**. `draft 21`
carries `visual_family=a2bcf8e2, visual_version=1` — the historical proof the row was really
used. The `WRITABLE_SLOT_TYPES` change is the only one of the three that actually closes it.

*Not fixed, deliberately:* `_resolve` accepting a retired or superseded template by id is a
second defect on the same path and outside this task. Worth a slice.

**`WRITABLE_SLOT_TYPES` — `""` is gone. An untyped slot is not writable.**
It was the whole mechanism: every slot of every template authored before `type` existed
counted as prose-writable by default, which is how four untyped slots on an APPROVED template
let the model write "image CTR" into an `<img src>`.
**The guard deliberately does not key on the slot's name.** `left_image_url` reads as an
asset; `subject`, `logo` and `hero` do not. A name heuristic would reintroduce the same class
of silent wrongness it exists to close. Absent type means unknown, and unknown fails loud.
Checked against all five live visuals: 1101 and 1102 are fully typed and unaffected; 556 is
retired; 557 and 798 were the only rows relying on untyped-as-writable, both PROPOSED, both
`ai`, both a single `subject` slot — now typed `text` in place. Nothing APPROVED depended on it.

**Nothing pinned the `likes` hack.** Removing `post.likes = engaged_actions` left the suite
green, so a future edit could have reintroduced it silently. Added the assertion rather than
flipped one: `(likes, comments, shares, saves) == (0,0,0,0)` and `engaged_actions == 900`.
`add_manual_post` now stores its `engaged_actions` argument in `engaged_actions` only —
it arrives as a single total and there is nothing to split it into.

**`note` is accepted and discarded, and that is a compromise, not a choice.** Both options
are closed: dropping it from the signature breaks `main.ManualPostIn`'s `**model_dump()`
(off-limits — parallel workstream), and persisting it needs a `Post.note` column, i.e. a
migration (forbidden). Marked `ponytail:` with both halves of the upgrade path. The linter
flagging it is not `make check` — ruff selects `E,F,I,UP,B`, not `ARG` — so it is cosmetic.

Learned:

- **One fixture carried the bug's shape.** `test_generation.library()` declared
  `[{"name": "big_number"}, {"name": "headline"}]` — untyped — so two tests went red on the
  change. Correct churn, not a regression: the fixture was asserting the old default.
- **The regression test has to reproduce the prose, not the omission.** The first version of
  `test_an_untyped_template_fails_loudly...` passed before the fix, because `WRITTEN` simply
  omits `left_image_url` and a leftover placeholder already raises. Only feeding the model's
  invented `"image CTR"` through discriminates.
- **Structural idempotency beats a guard.** `correct_external_creator_metrics.py` selects on
  the username it overwrites, so the second run matches nothing by construction.
- Reassign a JSONB list, never mutate it — SQLAlchemy does not track the in-place edit and
  the commit is a no-op.
- The corpus explorer's account dropdown derives its options from the rows, so
  `External creator` disappeared from the UI with the data and no frontend change.

**Verified after the live correction:** `metric_snapshot` holds **zero** rows for post 2300 and
zero rows carrying `likes = 1240` anywhere. Snapshots are only written by a Zernio analytics
sync keyed on `late_post_id`, and this row is MANUAL with `late_post_id` NULL, so the false
figure never propagated to a second table. Worth checking rather than assuming — a corrected
`post` row with a stale snapshot beside it is the same lie, one join away.

*Noticed, not fixed — the corrected row is uniquely fragile.* It is now the only row where
`engaged_actions` (1240) exceeds `likes+comments+shares+saves` (0), because the total is known
and the breakdown is not. `upsert_linkedin_posts` matches **unscoped by source** on
`platform == 'linkedin'` (`corpus.py:389`) and then recomputes
`engaged_actions = likes + comments + shares + saves`, so a LinkedIn scrape that
content-matched this post would zero the one figure genuinely known. Matching another
creator's post while scraping Monte's profile is unlikely, and fixing it is outside this task
— but the whole point of the change was preserving that 1240, so the path is named here.

Verified against the running app: `/posts?account=Monte+Desai` 69, `Creator+inspiration` 15
(the legacy row folded in during the slice), `pixii.creates` 13, `Pixii_ai` 10, unfiltered
107. The corpus page server-renders 69 rows — the creator top of 4331 is absent from the
default view and Monte's 185 leads it — with five cohort options derived from the data. The
templates page tags 10 proposals `borrowed from a creator`; the other 37 rows carry no
`body.cohort` at all (extracted before the field existed, or hand-authored), so nothing is
claimed for them. `make check` green at 258 tests.

### US-001 — Design tokens and typography foundation (done 2026-07-30)

globals.css: 26 lines of create-next-app boilerplate -> a real token layer in Tailwind v4
`@theme`. Colour/space/radius/shadow/motion/type tokens, light + dark. Deleted the
`font-family: Arial` body override, so the Geist that layout.tsx has been loading all along
finally applies. scoreboard/page.tsx reskinned onto the tokens as the proof (read-only server
component, so nothing could regress functionally), recharts colours included.

**The accent was wrong in the PRD and is now resolved.** The spec carried `#E8590C` as an
explicit placeholder. The real brand palette lives in the `body->>'html'` of the APPROVED
VISUAL templates — the markup that actually renders Pixii's post visuals — and says:

    #F2610C accent (5 uses)   #111 display black (4)   #F5F0E8 cream (3)
    #E4DAC6 deeper tan (2)    #6B635A muted ink (1)

Prefer those over the spec's guesses for border (`#E4E1D8`) and muted (`#6F6C64`).

**Contrast was measured, not assumed, and it mattered.** `#F2610C` is 3.09:1 on `--bg` and
3.24:1 on `--surface` — it FAILS AA as text. So the brand orange stays a fill and a derived
`--accent-text: #ba4b09` (5.80 / 5.34 / 4.87) carries text on light. In dark, `--accent-text`
flips back to `#f2610c`, which clears AA there. `--chart-1` follows `--accent-text` so charts
stay legible in both themes. **Rule for later slices: never put `--accent` on text, never put
`--accent-text` on a fill.**

Cream stays a raised-surface token (`--surface-2`), never the page background — the PRD is
explicit that the UI takes the brand's temperature "without cosplaying as a post".

Verified: ruff, mypy, eslint, tsc green; 258 tests pass; `pnpm build` clean (7 routes).
Zero npm packages added — primitives are US-002's job.
